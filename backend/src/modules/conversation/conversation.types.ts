/**
 * 对话类型定义
 * 从共享类型中导出，确保前后端类型一致
 */

import {
  Conversation,
  ConversationMessage,
  ConversationDetail
} from '../../shared/types/conversation.types';

export type {
  Conversation,
  ConversationMessage,
  ConversationDetail
};
